En este ejercicio se pide:

  1. Crear las acciones necesarias para consultar al api.

  2. Crear un effect para llamar al api (definido en example.state.ts).

  3. Configurar el reducer para poder guardar los datos en el store Example.

  4. Crear un selector con el nombre GET_NAME, para poder recuperar el valor first_name.

  5. En el fichero app.component.ts, en el método ngOnInit, asociar dicho selector a la variable userData$.

  Deberá aparecer el nombre en la pantalla el nombre: Janet